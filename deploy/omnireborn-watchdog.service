[Unit]
Description=Check OmniReborn heartbeat and restart a stale worker
After=omnireborn.service

[Service]
Type=oneshot
ExecStart=/bin/bash -c '/opt/omnireborn/.venv/bin/python /opt/omnireborn/streamer.py --health-check --interval 60 || /bin/systemctl restart omnireborn.service'
